# Session handoff (for next chat)

## Repo / branch
- Project: `mvp2` → remote `hsr_beta` (`main`)
- Recent commits: security RLS + public pack RPCs; agent login `role` in OTP; auth callback PKCE + error surfacing; **agencies FK insert order fix** (`ce14c4e`)

## Security (pre-deploy checklist — done in code)
- Supabase URL/anon key from env only (`src/lib/supabase.ts`)
- RLS migration: `supabase/migrations/20250326120000_enable_rls_public_pack_rpcs.sql` (+ earlier `increment_share_view`)
- Public pack: RPCs `get_public_pack_share_property`, `get_public_pack_details`, `register_public_pack_viewer`; document downloads via `/api/sign-pack-document` (24h signed URLs)
- `SIGNED_DOCUMENT_URL_TTL_SECONDS` in `src/lib/storageSignedUrl.ts`

## Auth / agent login
- **Login** sends `data: { role: 'agent' | 'seller' }` in `signInWithOtp` so metadata matches the tile chosen
- **`ensureUserProfile`**: normalize roles; **`public.users` must be inserted before `agencies`** — `agencies.agent_user_id` FK → `public.users`, not `auth.users`
- **AuthCallback**: explicit `exchangeCodeForSession` when needed; errors via `formatCallbackError`

## Known issues / next steps
- Apply Supabase migrations on the linked project before relying on RLS/RPCs in prod
- If `agency_id` is NOT NULL on `users` without default, agent insert may need a schema tweak

## To “save” this chat in Cursor
Use the chat panel’s history / export / copy; this file is a technical summary only.
